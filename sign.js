function validate()
{
    var username = document.getElementById('three');
    var username = document.getElementById('two');

    if(username.value=="" || password.value==" ")
    {
        alert("No blank values are allowed");
        return false;
    }
    else{
         true;
    }
}